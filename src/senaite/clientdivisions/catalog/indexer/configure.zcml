<configure
    xmlns="http://namespaces.zope.org/zope"
    i18n_domain="senaite.core">

  <!-- AnalysisRequest Indexer -->
  <adapter name="getDivisionUID" factory=".analysisrequest.division_uid"/>

  <!-- Batch Indexer -->
  <adapter name="getBatchDivisionUID" factory=".batch.batch_division_uid"/>

</configure>
